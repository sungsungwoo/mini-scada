# 이 파일은 참고용. 실제 값은 `frontend/.env.production` 에만 두면 됨 (커밋 가능).
#
# GitHub Actions: Secrets 없이 checkout 된 `.env.production` 으로 `docker build` 함.
#
# 대시보드 자동 새로고침 주기(초). 미설정 시 10.
# VITE_DASHBOARD_REFRESH_INTERVAL_SEC=10
